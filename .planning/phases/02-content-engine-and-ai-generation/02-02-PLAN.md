---
phase: 02-content-engine-and-ai-generation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(app)/layout.tsx
  - src/app/(app)/dashboard/page.tsx
  - src/components/layout/app-shell.tsx
  - src/components/layout/sidebar-nav.tsx
  - src/components/layout/bottom-nav.tsx
  - src/hooks/use-breakpoint.ts
autonomous: true
requirements: [UX-02, UX-05, UX-07, UX-08, UX-09]

must_haves:
  truths:
    - "Mobile shows bottom navigation bar at 64px height with touch targets >= 44x44px"
    - "Tablet shows icon-only sidebar at 80px width"
    - "Desktop shows expanded sidebar at 260px width with labels"
    - "All nav items are keyboard navigable with visible focus styles"
    - "Single Sidebar implementation replaces any parallel nav files"
  artifacts:
    - path: "src/components/layout/app-shell.tsx"
      provides: "Responsive app shell container"
      exports: ["AppShell"]
    - path: "src/components/layout/sidebar-nav.tsx"
      provides: "Sidebar for tablet and desktop"
      exports: ["SidebarNav"]
    - path: "src/components/layout/bottom-nav.tsx"
      provides: "Bottom navigation for mobile"
      exports: ["BottomNav"]
    - path: "src/hooks/use-breakpoint.ts"
      provides: "Breakpoint detection hook"
      exports: ["useBreakpoint"]
  key_links:
    - from: "src/components/layout/app-shell.tsx"
      to: "src/hooks/use-breakpoint.ts"
      via: "breakpoint hook for responsive rendering"
      pattern: "useBreakpoint"
    - from: "src/app/(app)/layout.tsx"
      to: "src/components/layout/app-shell.tsx"
      via: "layout renders AppShell"
      pattern: "AppShell"
---

<objective>
Build the responsive application shell with three navigation modes: bottom nav (mobile), icon sidebar (tablet), expanded sidebar (desktop). This is the single unified navigation per UX-09.

Purpose: Every page in the app renders inside this shell. The responsive layout must exist before content pages (create, library, planner) can be built.
Output: AppShell component with responsive breakpoint switching, SidebarNav, BottomNav, and useBreakpoint hook.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-content-engine-and-ai-generation/02-CONTEXT.md
@.planning/phases/02-content-engine-and-ai-generation/02-RESEARCH.md

<interfaces>
<!-- Existing app layout (will be modified) -->
From src/app/(app)/layout.tsx:
Uses getCurrentUser() for auth check, wraps children in app providers.

<!-- Existing sidebar component (to be replaced per UX-09) -->
From src/components/ui/sidebar.tsx:
Radix-based sidebar primitive -- may be used as foundation or replaced.

<!-- Existing mobile detection -->
From src/hooks/use-mobile.tsx:
```typescript
export function useIsMobile(): boolean
// Uses 768px breakpoint
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Breakpoint hook and responsive navigation components</name>
  <files>src/hooks/use-breakpoint.ts, src/components/layout/sidebar-nav.tsx, src/components/layout/bottom-nav.tsx</files>
  <read_first>
    - src/hooks/use-mobile.tsx
    - src/components/ui/sidebar.tsx
    - src/app/(app)/layout.tsx
    - src/app/(app)/dashboard/page.tsx
  </read_first>
  <action>
**1. Create `src/hooks/use-breakpoint.ts`:**
- `'use client'` hook
- Export `useBreakpoint()` returning `{ breakpoint: 'mobile' | 'tablet' | 'desktop', isMobile: boolean, isTablet: boolean, isDesktop: boolean }`
- Breakpoints: mobile < 640px, tablet 640-1023px, desktop >= 1024px
- Use `window.matchMedia` with `useEffect` listeners for changes
- Handle SSR with default `'desktop'` (server render) -- client hydrates immediately
- Keep `use-mobile.tsx` as-is for backward compatibility

**2. Create `src/components/layout/sidebar-nav.tsx`:**
- `'use client'` component
- Props: `{ collapsed?: boolean }` -- collapsed=true for tablet icon-only (80px), collapsed=false for desktop expanded (260px)
- Navigation items array (hardcoded for now, extensible later):
  ```
  { label: 'Dashboard', href: '/dashboard', icon: LayoutDashboard }
  { label: 'Create', href: '/create', icon: PlusCircle }
  { label: 'Planner', href: '/planner', icon: Calendar }
  { label: 'Library', href: '/library', icon: Image }
  { label: 'Campaigns', href: '/campaigns', icon: Megaphone }
  { label: 'Settings', href: '/settings', icon: Settings }
  ```
- Use Lucide React icons for each item
- Active state: highlight current route using `usePathname()` from `next/navigation`
- Keyboard navigation: all items are `<Link>` elements (inherently focusable), add `aria-current="page"` on active item (UX-07)
- Focus styles: `focus-visible:ring-2 focus-visible:ring-ring focus-visible:ring-offset-2` on all links
- When collapsed: show only icon centered, tooltip on hover using existing Radix Tooltip
- When expanded: icon + label, with label text
- Fixed position left side, full height, border-right
- Per D-08 bold/branded: use primary colour for active item background, bold icon weight

**3. Create `src/components/layout/bottom-nav.tsx`:**
- `'use client'` component
- Fixed bottom, 64px height (per UX-02), full width, border-top
- Show 5 nav items: Dashboard, Create (center, larger), Planner, Library, Settings
- Each item: icon + small label below, minimum touch target 44x44px (UX-05) via `min-h-11 min-w-11`
- Active state via `usePathname()`
- `aria-current="page"` on active item
- Focus styles matching sidebar
- Create button in center position visually distinct (primary colour background, slightly raised)
  </action>
  <verify>
    <automated>cd /Users/peterpitcher/Cursor/OJ-CheersAI2.0 && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/hooks/use-breakpoint.ts contains `export function useBreakpoint`
    - src/hooks/use-breakpoint.ts contains `'mobile' | 'tablet' | 'desktop'`
    - src/hooks/use-breakpoint.ts contains `640`
    - src/hooks/use-breakpoint.ts contains `1024`
    - src/components/layout/sidebar-nav.tsx contains `export function SidebarNav`
    - src/components/layout/sidebar-nav.tsx contains `'use client'`
    - src/components/layout/sidebar-nav.tsx contains `collapsed`
    - src/components/layout/sidebar-nav.tsx contains `aria-current`
    - src/components/layout/sidebar-nav.tsx contains `focus-visible`
    - src/components/layout/sidebar-nav.tsx contains `260`
    - src/components/layout/sidebar-nav.tsx contains `80`
    - src/components/layout/bottom-nav.tsx contains `export function BottomNav`
    - src/components/layout/bottom-nav.tsx contains `min-h-11`
    - src/components/layout/bottom-nav.tsx contains `64`
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>Breakpoint hook detects mobile/tablet/desktop. SidebarNav renders collapsed (80px icon-only) and expanded (260px with labels) modes. BottomNav renders 64px fixed bottom bar with 44px touch targets.</done>
</task>

<task type="auto">
  <name>Task 2: AppShell wiring and layout integration</name>
  <files>src/components/layout/app-shell.tsx, src/app/(app)/layout.tsx, src/app/(app)/dashboard/page.tsx</files>
  <read_first>
    - src/app/(app)/layout.tsx
    - src/app/(app)/dashboard/page.tsx
    - src/components/layout/sidebar-nav.tsx
    - src/components/layout/bottom-nav.tsx
    - src/hooks/use-breakpoint.ts
  </read_first>
  <action>
**1. Create `src/components/layout/app-shell.tsx`:**
- `'use client'` component
- Props: `{ children: React.ReactNode }`
- Import `useBreakpoint` from `@/hooks/use-breakpoint`
- Import `SidebarNav` from `@/components/layout/sidebar-nav`
- Import `BottomNav` from `@/components/layout/bottom-nav`
- Rendering logic:
  - mobile (< 640px): No sidebar. Content area fills full width. BottomNav at bottom. Content has `pb-16` padding-bottom to clear bottom nav.
  - tablet (640-1023px): SidebarNav collapsed (80px) on left. Content area with `pl-20` (80px left padding). No bottom nav.
  - desktop (>= 1024px): SidebarNav expanded (260px) on left. Content area with `pl-[260px]`. No bottom nav.
- Main content wrapper: `<main>` with `min-h-screen` and appropriate padding
- Add Framer Motion `AnimatePresence` for smooth nav transitions when breakpoint changes (per D-10). Use `layout` prop on sidebar for smooth width animation.

**2. Update `src/app/(app)/layout.tsx`:**
- Replace current layout wrapper with `<AppShell>{children}</AppShell>`
- Keep `getCurrentUser()` auth check at top
- Keep existing provider wrapping
- Remove any old sidebar/nav rendering that existed
- This becomes: auth check -> providers -> AppShell -> children

**3. Update `src/app/(app)/dashboard/page.tsx`:**
- Keep existing welcome content
- Wrap in a container div: `max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-6`
- This ensures the dashboard content respects the shell's padding
  </action>
  <verify>
    <automated>cd /Users/peterpitcher/Cursor/OJ-CheersAI2.0 && npx tsc --noEmit 2>&1 | tail -5 && npm run lint 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/components/layout/app-shell.tsx contains `export function AppShell`
    - src/components/layout/app-shell.tsx contains `'use client'`
    - src/components/layout/app-shell.tsx contains `useBreakpoint`
    - src/components/layout/app-shell.tsx contains `SidebarNav`
    - src/components/layout/app-shell.tsx contains `BottomNav`
    - src/components/layout/app-shell.tsx contains `pb-16`
    - src/components/layout/app-shell.tsx contains `260`
    - src/app/(app)/layout.tsx contains `AppShell`
    - src/app/(app)/layout.tsx contains `getCurrentUser`
    - npx tsc --noEmit exits 0
    - npm run lint exits 0
  </acceptance_criteria>
  <done>AppShell renders responsive layout: bottom nav on mobile, 80px icon sidebar on tablet, 260px expanded sidebar on desktop. App layout wired through AppShell. Dashboard content respects shell padding.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- `npm run lint` passes
- `grep -l "AppShell" src/app/\(app\)/layout.tsx` returns match
- `grep "SidebarNav\|BottomNav" src/components/layout/app-shell.tsx | wc -l` returns 2+
- `grep "useBreakpoint" src/components/layout/app-shell.tsx` returns match
</verification>

<success_criteria>
- Single unified navigation implementation (UX-09) -- no parallel nav files
- Mobile: bottom nav at 64px with 44px touch targets (UX-02, UX-05)
- Tablet: icon sidebar at 80px (UX-02)
- Desktop: expanded sidebar at 260px (UX-02)
- All nav items keyboard navigable with focus styles (UX-07)
- AppShell wired into app layout with auth guard intact
</success_criteria>

<output>
After completion, create `.planning/phases/02-content-engine-and-ai-generation/02-02-SUMMARY.md`
</output>
