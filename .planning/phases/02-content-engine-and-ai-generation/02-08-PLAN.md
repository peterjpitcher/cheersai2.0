---
phase: 02-content-engine-and-ai-generation
plan: 08
type: execute
wave: 5
depends_on: [02-04, 02-05, 02-06, 02-07]
files_modified:
  - src/features/create/steps/generate-step.tsx
  - src/features/create/steps/media-step.tsx
  - src/features/create/steps/schedule-step.tsx
  - src/features/create/create-wizard.tsx
autonomous: false
requirements: [UX-09]

must_haves:
  truths:
    - "Generate step calls AI generation server action and displays platform columns with generated copy"
    - "Modifier chips trigger regeneration with the selected modifier"
    - "Media step uses the media picker to browse, upload, and attach media"
    - "Schedule step shows conflict detection warnings from the scheduling engine"
    - "Full wizard flow works end-to-end: Brief -> Generate -> Media -> Schedule"
  artifacts:
    - path: "src/features/create/steps/generate-step.tsx"
      provides: "AI generation wired into wizard step"
    - path: "src/features/create/steps/media-step.tsx"
      provides: "Media picker wired into wizard step"
    - path: "src/features/create/steps/schedule-step.tsx"
      provides: "Schedule step with conflict warnings"
  key_links:
    - from: "src/features/create/steps/generate-step.tsx"
      to: "src/app/actions/ai-generate.ts"
      via: "generateContent and regenerateWithModifier server actions"
      pattern: "generateContent"
    - from: "src/features/create/steps/media-step.tsx"
      to: "src/features/create/media/media-picker.tsx"
      via: "MediaPicker component"
      pattern: "MediaPicker"
    - from: "src/features/create/steps/schedule-step.tsx"
      to: "src/lib/scheduling/conflicts.ts"
      via: "detectConflicts for warning display"
      pattern: "detectConflicts"
---

<objective>
Wire all wizard steps to their backend implementations: AI generation into Generate step, media library into Media step, and conflict detection into Schedule step. Produce a working end-to-end create flow.

Purpose: Plans 04-07 built the individual pieces. This plan connects them into a cohesive wizard experience that an owner can use to create content from start to finish.
Output: Fully functional create wizard with AI generation, media attachment, and scheduling.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-content-engine-and-ai-generation/02-CONTEXT.md
@.planning/phases/02-content-engine-and-ai-generation/02-RESEARCH.md
@.planning/phases/02-content-engine-and-ai-generation/02-04-SUMMARY.md
@.planning/phases/02-content-engine-and-ai-generation/02-05-SUMMARY.md
@.planning/phases/02-content-engine-and-ai-generation/02-06-SUMMARY.md
@.planning/phases/02-content-engine-and-ai-generation/02-07-SUMMARY.md

<interfaces>
<!-- From Plan 02-05 (AI generation) -->
From src/app/actions/ai-generate.ts:
```typescript
export async function generateContent(contentId: string, brief: ContentBrief): Promise<{ data?: PostprocessResult; error?: string }>
export async function regenerateWithModifier(contentId: string, brief: ContentBrief, modifier: string): Promise<{ data?: PostprocessResult; error?: string }>
```

<!-- From Plan 02-06 (Media) -->
From src/features/create/media/media-picker.tsx:
```typescript
export function MediaPicker({ selectedMediaIds, onMediaChange, campaignName }: Props): JSX.Element
```

From src/app/actions/media.ts:
```typescript
export async function attachMediaToContent(contentItemId: string, mediaIds: string[]): Promise<{ success?: boolean; error?: string }>
```

<!-- From Plan 02-07 (Scheduling) -->
From src/lib/scheduling/conflicts.ts:
```typescript
export function detectConflicts(items: ContentItem[]): Conflict[]
```

<!-- From Plan 02-04 (Wizard steps - current state) -->
From src/features/create/steps/generate-step.tsx:
```typescript
// Currently a placeholder -- needs AI wiring
export function GenerateStep({ contentBrief, generatedCopy, onGenerate, onRegenerate, isGenerating }: Props): JSX.Element
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire AI generation into generate step and media picker into media step</name>
  <files>src/features/create/steps/generate-step.tsx, src/features/create/steps/media-step.tsx, src/features/create/create-wizard.tsx</files>
  <read_first>
    - src/features/create/steps/generate-step.tsx
    - src/features/create/steps/media-step.tsx
    - src/features/create/create-wizard.tsx
    - src/app/actions/ai-generate.ts
    - src/features/create/media/media-picker.tsx
    - src/app/actions/media.ts
    - src/types/content.ts
    - src/lib/ai/voice.ts
  </read_first>
  <action>
**1. Update `src/features/create/steps/generate-step.tsx`:**

Wire the placeholder to call real AI generation. The component already has the UI structure (platform columns, modifier chips) from Plan 04. Now add:

- Import `generateContent` and `regenerateWithModifier` from `@/app/actions/ai-generate`
- Add `useMutation` from React Query for generation calls
- **onGenerate handler**: Calls `generateContent(contentId, brief)`, sets `generatedCopy` state on success, shows toast error on failure
- **onRegenerate handler**: Calls `regenerateWithModifier(contentId, brief, modifier)` with the modifier string from the clicked chip
- **Loading state**: Show skeleton loaders in platform columns during `isGenerating`
- **Error state**: Show error message with "Try Again" button. If timeout error (AI-09): "Generation took too long -- please try again with a simpler brief."
- **Generated state**: Platform columns now populated with real data from `PostprocessResult.copy`
  - Facebook column: body textarea, CTA text display, hashtags as chips
  - Instagram column: body textarea, hashtags as chips, link-in-bio line
  - GBP column: body textarea, CTA action type display
- **Warnings display**: Show `PostprocessResult.warnings` as amber alert banners below the columns (e.g. AI-08 GBP CTA warning)
- **Edit capability**: All body text fields are editable textareas. Owner can manually tweak after generation.
- **Modifier chips** (per D-06): Below generated content, render the 6 chips:
  ```
  { id: 'shorter', label: 'Make shorter', modifier: 'Make the copy shorter and more concise.' }
  { id: 'formal', label: 'More formal', modifier: 'Increase formality while keeping warmth.' }
  { id: 'emoji', label: 'Add emoji', modifier: 'Add relevant emojis to enhance the message.' }
  { id: 'cta', label: 'Stronger CTA', modifier: 'Strengthen the call-to-action with more urgency.' }
  { id: 'casual', label: 'More casual', modifier: 'Make the tone more relaxed and conversational.' }
  { id: 'longer', label: 'More detail', modifier: 'Add more specific details and description.' }
  ```
  Each chip click triggers regeneration with that modifier.

**2. Update `src/features/create/steps/media-step.tsx`:**

Replace the placeholder with the real MediaPicker:

- Import `MediaPicker` from `@/features/create/media/media-picker`
- Import `attachMediaToContent` from `@/app/actions/media`
- Render `<MediaPicker selectedMediaIds={selectedMediaIds} onMediaChange={handleMediaChange} campaignName={brief.title} />`
- On step leave (when wizard advances to Schedule step): call `attachMediaToContent(contentId, selectedMediaIds)` to persist the attachment

**3. Update `src/features/create/create-wizard.tsx`:**

Wire the wizard to pass real callbacks to each step:

- Add state for `generatedCopy: PlatformCopy | null` and `isGenerating: boolean`
- Pass `onGenerate` and `onRegenerate` callbacks to GenerateStep
- Pass `selectedMediaIds` and `onMediaChange` to MediaStep
- On wizard completion (Schedule step confirm): call `saveDraft` with final state including generated copy and media
- Add error boundary around each step for graceful failure
  </action>
  <verify>
    <automated>cd /Users/peterpitcher/Cursor/OJ-CheersAI2.0 && npx tsc --noEmit 2>&1 | tail -5 && npm run lint 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/features/create/steps/generate-step.tsx contains `generateContent`
    - src/features/create/steps/generate-step.tsx contains `regenerateWithModifier`
    - src/features/create/steps/generate-step.tsx contains `useMutation`
    - src/features/create/steps/generate-step.tsx contains `Make shorter`
    - src/features/create/steps/generate-step.tsx contains `warnings`
    - src/features/create/steps/media-step.tsx contains `MediaPicker`
    - src/features/create/steps/media-step.tsx contains `attachMediaToContent`
    - src/features/create/create-wizard.tsx contains `generatedCopy`
    - src/features/create/create-wizard.tsx contains `isGenerating`
    - npx tsc --noEmit exits 0
    - npm run lint exits 0
  </acceptance_criteria>
  <done>Generate step calls AI actions and displays platform columns with real data. Modifier chips trigger regeneration. Warnings displayed. Media step uses MediaPicker and persists attachments. Wizard passes real callbacks to all steps.</done>
</task>

<task type="auto">
  <name>Task 2: Wire schedule step to conflict detection and finalize wizard flow</name>
  <files>src/features/create/steps/schedule-step.tsx</files>
  <read_first>
    - src/features/create/steps/schedule-step.tsx
    - src/lib/scheduling/conflicts.ts
    - src/lib/content/queries.ts
    - src/types/content.ts
    - src/app/actions/content.ts
  </read_first>
  <action>
**1. Update `src/features/create/steps/schedule-step.tsx`:**

Wire the schedule step to conflict detection and finalize the create flow:

- Import `detectConflicts` from `@/lib/scheduling/conflicts`
- Import `getContentForCalendar` from `@/lib/content/queries` (via a server action wrapper for client-side use)
- On date/time selection: fetch existing scheduled items for that day's 6-hour window, run `detectConflicts()` against the new item + existing items
- **Conflict display**: If conflicts detected, show amber warning card:
  - "Scheduling conflict: {itemA.title} is already scheduled at {time} on {platform}"
  - Show suggestion: "Consider moving to {suggestedTime}"
  - Allow owner to proceed anyway (not blocking, just warning per SCHED-02)
- **Summary section**: Show complete content summary before confirmation:
  - Content type badge
  - Title
  - Selected platforms with PlatformBadge
  - Generated copy preview (truncated)
  - Attached media thumbnails
  - Scheduled date/time in Europe/London timezone
- **Confirm actions**:
  - "Save as Draft": updates status to 'draft', closes wizard
  - "Schedule": validates date is in the future, updates content item with `scheduled_at` and status 'scheduled', closes wizard
  - For instant posts with `publishMode: 'now'`: "Save and Queue" button (sets status to 'approved' for Phase 4 pipeline)
- **Timezone note**: Small text below date picker: "All times are in Europe/London (GMT/BST)"
- Use Luxon for all date display formatting: `DateTime.fromJSDate(date, { zone: 'Europe/London' }).toFormat('dd MMM yyyy, HH:mm')`
  </action>
  <verify>
    <automated>cd /Users/peterpitcher/Cursor/OJ-CheersAI2.0 && npx tsc --noEmit 2>&1 | tail -5 && npm run lint 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/features/create/steps/schedule-step.tsx contains `detectConflicts`
    - src/features/create/steps/schedule-step.tsx contains `Scheduling conflict`
    - src/features/create/steps/schedule-step.tsx contains `Europe/London`
    - src/features/create/steps/schedule-step.tsx contains `Save as Draft`
    - src/features/create/steps/schedule-step.tsx contains `Schedule`
    - src/features/create/steps/schedule-step.tsx contains `PlatformBadge`
    - src/features/create/steps/schedule-step.tsx contains `DateTime`
    - npx tsc --noEmit exits 0
    - npm run lint exits 0
  </acceptance_criteria>
  <done>Schedule step shows conflict warnings with resolution suggestions, content summary, and confirm/schedule actions. All times displayed in Europe/London timezone.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: End-to-end phase verification</name>
  <action>Human verification of the complete Phase 2 deliverable: responsive layout, dark mode, create wizard (5 content types), AI generation, media library, planner calendar, and scheduling.</action>
  <what-built>Complete end-to-end create wizard: Brief (type selection, fields, fine-tune controls) -> Generate (AI platform columns, modifier chips) -> Media (upload, browse, attach) -> Schedule (conflict detection, confirmation). Full responsive layout with design system tokens and dark mode.</what-built>
  <how-to-verify>
    1. Run `npm run dev`
    2. Sign in and navigate to Dashboard
    3. Test responsive layout:
       - Resize browser to mobile width: should see bottom nav
       - Resize to tablet width: should see icon-only sidebar
       - Full desktop: should see expanded sidebar with labels
    4. Toggle dark mode (if toggle exists in settings or use browser devtools)
    5. Click "Create" in navigation
    6. Select "Instant Post" content type
    7. Fill in title and brief, select Facebook and Instagram platforms
    8. Expand "Advanced Options" -- verify tone dropdown shows 5 hospitality options
    9. Click "Next" -- verify draft saves (check content_items table)
    10. On Generate step: click "Generate" -- verify AI generates platform-specific copy (requires OPENAI_API_KEY in .env)
    11. Click a modifier chip (e.g. "Make shorter") -- verify regeneration
    12. Click "Next" to Media step -- verify media picker shows
    13. Upload an image -- verify it appears in picker
    14. Click "Next" to Schedule step -- verify summary and date picker
    15. Schedule for a time -- verify conflict detection works if another item exists nearby
    16. Visit /planner -- verify calendar shows scheduled content
    17. Visit /library -- verify standalone media library page works
    18. Click a calendar item -- verify side drawer opens on desktop
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- `npm run lint` passes
- `npm test` passes
- Full wizard flow: Brief -> Generate -> Media -> Schedule completes without errors
- All 8 plans' code integrates cleanly
</verification>

<success_criteria>
- Complete end-to-end create flow working for all 5 content types
- AI generation produces real platform-specific copy
- Modifier chips regenerate with instruction
- Media upload and attachment functional
- Schedule step shows conflict warnings
- Responsive layout works across mobile/tablet/desktop
- Dark mode functional
- Planner calendar displays scheduled content
- Side drawer opens for post detail on desktop
</success_criteria>

<output>
After completion, create `.planning/phases/02-content-engine-and-ai-generation/02-08-SUMMARY.md`
</output>
